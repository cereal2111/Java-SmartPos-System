<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.StackPane?>

<BorderPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
    fx:controller="io.smartpos.ui.main.MainLayoutController"
    stylesheets="@../css/style.css">

    <!-- Left Sidebar -->
    <left>
        <VBox styleClass="sidebar" prefWidth="250" spacing="10">
            <Label text="SmartPOS" styleClass="app-title" />

            <Button fx:id="btnSale" text="🛒 Point of Sale" onAction="#showSale"
                styleClass="nav-button" maxWidth="Infinity" />

            <Button fx:id="btnHistory" text="📜 Sale History" onAction="#showHistory"
                styleClass="nav-button" maxWidth="Infinity" />

            <Button fx:id="btnProducts" text="📦 Products" onAction="#showProducts"
                styleClass="nav-button" maxWidth="Infinity" />

            <Button fx:id="btnPurchases" text="🚚 Restocking" onAction="#showPurchases"
                styleClass="nav-button" maxWidth="Infinity" />

            <Button fx:id="btnInventory" text="📊 Reports" onAction="#showReports"
                styleClass="nav-button" maxWidth="Infinity" />

            <Button fx:id="btnCatalogs" text="🗂️ Catalogs" onAction="#showCatalogs"
                styleClass="nav-button" maxWidth="Infinity" />

            <Button fx:id="btnCash" text="💰 Cash Closure" onAction="#showCashClosure"
                styleClass="nav-button" maxWidth="Infinity" />

            <VBox VBox.vgrow="ALWAYS" /> <!-- Spacer -->

            <Button fx:id="btnSettings" text="⚙️ Settings" styleClass="nav-button"
                maxWidth="Infinity" />
        </VBox>
    </left>

    <!-- Top Header and Content -->
    <center>
        <BorderPane>
            <top>
                <HBox styleClass="header" alignment="CENTER_LEFT">
                    <Label fx:id="pageTitle" text="Dashboard" styleClass="page-title" />
                    <HBox HBox.hgrow="ALWAYS" /> <!-- Spacer -->
                    <HBox spacing="15" alignment="CENTER_RIGHT">
                        <VBox alignment="CENTER_RIGHT">
                            <Label fx:id="userNameLabel" text="User Name"
                                style="-fx-font-weight: bold; -fx-text-fill: #1e293b;" />
                            <Label fx:id="userRoleLabel" text="Role"
                                style="-fx-font-size: 11px; -fx-text-fill: #64748b;" />
                        </VBox>
                        <Button text="🚪" onAction="#handleLogout"
                            style="-fx-background-color: #f1f5f9; -fx-background-radius: 50; -fx-cursor: hand;" />
                    </HBox>
                </HBox>
            </top>
            <center>
                <StackPane fx:id="contentArea" styleClass="content-area" />
            </center>
        </BorderPane>
    </center>

</BorderPane>